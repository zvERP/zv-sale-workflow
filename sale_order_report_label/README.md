# Sale Order Report Label

This module lets users manually override the sales order report header text in specific cases.

## What it does

- Adds an editable field on the order to define an alternative report title.
- If the field is empty, the PDF keeps the standard Odoo title.
- If the field has a value, it replaces the normal header text before the order number.
