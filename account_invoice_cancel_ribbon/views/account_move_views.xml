<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_inherit_cancel_ribbon" model="ir.ui.view">
        <field name="name">account.move.form.cancel.ribbon</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//form/sheet" position="inside">
                <widget
                    name="web_ribbon"
                    title="ANULADA"
                    bg_color="bg-danger"
                    attrs="{'invisible': ['|', ('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund')), ('state', '!=', 'cancel')]}"
                />
            </xpath>
        </field>
    </record>
</odoo>
