# Sale Estimated Collection Date

This module adds an estimated collection date to sales orders and uses it as a commercial control field.

## What it does

- Adds the `Estimated collection date` field to the sales order.
- Prevents order confirmation when that field is empty.
- Adds views to review orders by that date, including calendar and filters.
